export interface WebsiteLocale {
    landing: {
        newPost: string
    }
    editPost: {
        newPostHeading: string
        placeholder: string
        override: string
        publish: string
    }
}
